{% extends 'admin/templates/default.html.twig' %}

{% block content %}
    <h1>Edit recette</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('admin_recipe_list') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
